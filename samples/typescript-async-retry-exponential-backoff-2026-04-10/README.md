# typescript-async-retry-exponential-backoff

記事「TypeScript で async 関数を指数バックオフ+ジッターでリトライする汎用ヘルパー」のサンプルコード。

## セットアップ

```bash
npm install
```

## テスト実行

```bash
npm test
```

`node --test` + `tsx` で、TypeScript のテストをビルド無しで実行します。
